# Active Work

## Current Branch

- Active branch: `none`.
- Current branch: `main`.
- Last completed work: first-login password session stabilization plus AI entry points, structured AI context for management tasks/meetings, and the first human-confirmed AI work-record actions. The first-login password path now returns a full normalized `app_users` profile from `createSupabaseInitialPasswordClient.completePasswordUser()` instead of a partial `{ authUserId, appUserId }` payload, so the immediate post-password session shape matches the normal `/api/session/me` / login contract and avoids white-screen risk from an incomplete production session. `tests/initialPasswordHandler.test.js` now covers role, name, department, departments, permissions, and app user id after first-password completion. AI assist now also has a management tasks/meetings prompt entry point (`managementTasksAiPrompt`) wired from `ManageHub` for both admin and manager task screens. `src/AIPanel.jsx` passes tasks and meetings into `buildAIContextSnapshot()`, `src/aiAssistSnapshotModel.js` includes compact open/overdue/waiting task and planned/needs-summary meeting records, and `src/aiAssistContextModel.js` applies role filtering and sanitization before `/api/ai/assist` sends context to a provider; meeting context now preserves the safe `at` timestamp so update previews can show the old and new time. AI audit events count tasks/meetings without storing their raw titles. Work-record AI actions now include `task.create`, constrained `task.update`, `meeting.create`, and constrained `meeting.update`: deterministic task/meeting intake produces human-confirmed actions for `/api/work`, and the browser executes them only through the existing `saveTask` / `saveMeeting` paths after user confirmation. `task.update` is intentionally narrow for now: status, priority, and explicit due-date changes (`today` / `tomorrow` / `in N days`, Hebrew equivalents, and explicit calendar dates such as `15.07.26` or `15/07/2026`), one visible task only, no ambiguous date guessing, and no guessing between multiple task targets. `meeting.create` is also intentionally narrow: it creates a planned meeting with the current actor as owner/participant and an explicit relative or calendar date/time, with `HH:mm` overriding the time when present. `meeting.update` currently changes only `at` for exactly one visible meeting from explicit relative or calendar date/time wording. AI does not infer rooms, extra participants, recurring rules, meeting deletes, or ambiguous meeting targets. Earlier AI assist work still applies: deterministic `ticket.create`, `ticket.update`, `ticket.comment`, supplier-routing, and waiting-reason proposals require human confirmation and execute only through normal validated app operations. Future write-capable AI operations must stay deterministic, permission-filtered, audited, and human-confirmed.
- Current work: none.

## Current Product Direction

- Keep the v1 release focused on working, low-risk areas:
  - tickets;
  - fleet and transport records;
  - periodic maintenance;
  - cleaning operations;
  - PPE;
  - tasks, meetings, users, suppliers, settings, audit, and production login.
- Do not reintroduce the removed experimental module direction in this release.
- Do not create new `src/app`, `src/features`, or `src/shared` structure in v1 cleanup work.
- Do not touch v2 or Claude branches from this workstream.

## Notes For Next Agent

- Treat removed storage prefixes and docs as intentionally retired, not as migration backlog.
- Use `npm run staging:kv:retire-mirrors -- --prefix <prefix>` as a dry-run before deleting old compatibility mirrors; `-- --prefix <prefix> --apply` deletes only shared KV records whose key matches a normalized table row's `source_kv_key`.
- Do not invent broad product-polish backlog from screenshots or old notes. Wait for a concrete owner-reported issue.
- If the owner continues visual polish in a new session, use `docs/handoff-for-next-codex.md` as the current UI direction and strategy. Continue with small, scoped screen-by-screen patches and browser/mobile verification.
- For performance work, do not treat the `content-visibility`, `html2canvas`, vendor-chunk split, lazy AI panel, lazy notification panel, lazy admin ticket list, lazy suppliers panel, lazy activity log, lazy PPE dashboard, lazy PPE order UI, lazy management tasks/meetings hub, lazy settings/team panel, lazy ticket detail overlay, lazy BI overview, lazy fleet/transport/PM module, or lazy app-issues settings screen as the final fix. They reduce rendering pressure, remove rarely used code from startup, and make React/icons more cacheable across deploys, but the total first-load JavaScript remains too large. The next meaningful step is another scoped lazy split of one remaining heavier screen group, preferably a self-contained operational module.
- For AI work, `/api/ai/assist` is now the safe server boundary and remains read-only by itself. The settings/UI slice lets admins select provider/mode/model state without exposing secrets, checks `/api/ai/status` for server readiness, and can run an admin-only `/api/ai/status?check=1` live provider ping from the server. Provider aliases are server-side and operator-friendly: `claude` maps to Anthropic, while `codex`, `chatgpt`, and `openai` map to the OpenAI Responses adapter. The AI panel can call the server assistant with permission-filtered context, provider calls write non-sensitive `ai_assist` audit events when the audit driver is configured, and first workflow modes exist for risk summary, SLA explanation, next actions, and draft preparation. AI context can include compact role-filtered BI heatmap summaries (`bi.heatmap`) so the assistant can explain risk concentration without receiving unrelated department detail. It can also include compact supplier summaries for roles/users with supplier visibility, but without supplier contacts, addresses, or private notes. AI context now includes compact role-filtered management tasks and meetings: open/overdue/waiting tasks, due days, exact due timestamp when present, responsible ids, waiting reason, linked meeting id, planned meetings, needs-summary meetings, open task counts, and meeting `at` timestamps for safe confirmation previews. Role-specific AI welcome text and quick workflow prompts live in `src/aiAssistQuickPromptModel.js`; the context snapshot shape lives in `src/aiAssistSnapshotModel.js`; dedicated UI entry points live in `src/aiAssistEntryPointModel.js`, including ticket, fleet, cleaning, PPE, supplier, BI heatmap, and management task/meeting prompts. Executable AI actions now include `ticket.create`, `ticket.update`, `ticket.comment`, `task.create`, constrained `task.update`, `meeting.create`, and constrained `meeting.update`, but every one remains non-writing until a user confirms it in the UI. `ticket.create` runs only through `saveTicket` / `/api/tickets`; incomplete/review-first ticket proposals can open the normal `TicketForm` with `ticketPrefillFromAiAssistAction()` output. `ticket.update` applies only allow-listed fields, requires a matching existing ticket, produces a diff, appends an `ai_confirmed_update` log entry, and then uses `saveTicket`. Supplier routing requires an explicit supplier name match from the filtered supplier context; if the supplier is invisible, ambiguous, unchanged, or absent, no proposal is generated. `ticket.comment` appends an `ai_confirmed_comment` log entry only after confirmation and then uses `saveTicket`. `task.create` runs only through `saveTask` / `/api/work` with `resource: "tasks"`, adds an `ai_confirmed_task` log entry, and `task.update` currently changes only status/priority/dueAt for exactly one visible task, appending `ai_confirmed_task_update`; due-date changes are accepted from explicit relative wording such as today/tomorrow/in N days and explicit calendar dates in `DD.MM.YY`, `DD.MM.YYYY`, `DD/MM/YY`, or `DD/MM/YYYY` formats. `meeting.create` runs only through `saveMeeting` / `/api/work` with `resource: "meetings"` and adds `ai_confirmed_meeting` metadata. `meeting.update` currently changes only `at` for exactly one visible meeting, appends `ai_confirmed_meeting_update`, and uses the same `saveMeeting` path; it accepts the same explicit relative/calendar date forms, with `HH:mm` setting the meeting time. Task responsible changes, task delete, meeting deletes, extra participants, rooms, and recurring meetings are still future separate safety slices. Add future write-capable AI operations one at a time through normal validated server operations with audit.
- Live AI connection status on 2026-07-13: authenticated `/api/ai/status` on Vercel initially returned `mode: "disabled"`, `serverReady: false`, and `errors: ["ai_server_disabled"]` because production AI env was absent. Production Vercel env was then configured with `CMMS_AI_MODE=server`, `CMMS_AI_PROVIDER=openai`, `CMMS_AI_MODEL`, and a sensitive `OPENAI_API_KEY`. Do not store or print the key in repo/docs. The first post-env provider check exposed a real OpenAI Responses API bug: the connection-check path used `max_output_tokens=8`, below OpenAI's minimum. `server/ai/statusHandler.js` now requests 16 tokens and `server/ai/providerClient.js` clamps OpenAI calls to at least 16. After the token-floor fix deployed, the live provider check reached OpenAI but returned `ai_provider_quota_exceeded` / quota-billing failure from the provider account. The app now normalizes that failure for settings and the AI panel instead of showing a generic network/offline error. To make live AI answer, the OpenAI project/account behind the configured key needs usable billing/quota or a different working server-side key.
- AI waiting-status proposals are intentionally stricter than generic status changes: the user must name a concrete waiting reason, the model adds a deterministic `waitingReason` and `waitBall`, generic waiting requests produce no proposal, and moving a waiting ticket back to work clears the waiting fields.
- AI ticket zone/location proposals are intentionally narrow: the user must explicitly ask to update a single role-visible ticket to an `אזור` / `איזור` / `מיקום` / `zone` / `location` value, and the assistant proposes only a `zone` patch through the existing human-confirmed `ticket.update` / `saveTicket` path. Free-text location mentions do not update existing tickets by themselves.
- AI transport-unit proposals are also intentionally narrow: the user must explicitly ask to update a single role-visible ticket to a specific `כלי` / `מלגזה` / `forklift` / `vehicle` / `unit` code that exists uniquely in the already role-filtered fleet context. The assistant can then propose `forkliftId` and `asset` together, still only as a human-confirmed `ticket.update` through the normal ticket save path. If the fleet code is hidden, ambiguous, unchanged, or absent from context, no action is proposed and the assistant must not fall back to creating a new ticket from update wording.
- The live deployment check after the latest stabilization confirmed Vercel served `bbd48e6`, `npm run staging:smoke:live -- --expect-current-commit` passed, and `npm run staging:smoke:ui -- --expect-current-commit` passed for desktop/mobile navigation after the BI overview split. Earlier live users/tickets/PIN API smokes and negative-path checks also passed: scoped managers cannot overwrite existing non-worker profiles, and executive login reset writes `reset_required` / `must_change_password`.
- Supplier-linked technicians are not a new table yet: the current contract uses the existing `app_users.supplier` / session `supplier` profile field, fleet `supplier`, `config.supplierMeta[name].industries`, and `ticket.supplier`. Ticket assignment should go to a supplier/contractor queue first; the concrete `assignee` is set only when one of that supplier's technicians accepts the ticket. If this grows into supplier-side dispatch workflows, extend that contract deliberately instead of adding a parallel assignment path.
- Tickets, fleet, periodic maintenance, users, cleaning, PPE, work records, settings records, app config, presence, push subscriptions, ticket photos, and the shared KV write guard are now closed for the current R10 production data-core scope. Do not open another R10 migration slice unless a live bug, an explicit new business domain, or independent review finding identifies one.
- User identity/session is already backed by Supabase `app_users` and `/api/session/me`, and `/api/users` now reads login-capable users from `app_users`, creates new users in `app_users`, writes expanded profile fields there, deactivates matching rows on delete, and resets login authority state on manager-triggered reset without creating new production/API `user:*` KV mirrors. `app_users` also carries PIN authority fields (`pin_hash`, `pin_updated_at`, `login_state`) so first-login/PIN setup, reset, and PIN session restore use `app_users` when configured. On 2026-07-10 staging backfilled the remaining legacy-only `user:` rows into `app_users`; after the user mirror guard was live, `npm run staging:users:retire-kv-mirrors -- --apply` deleted all 12 matched `user:*` mirrors. The follow-up residual report showed `cmms_kv_records=0`.
- Cleaning zones, rounds, complaints, and worker absences now use normalized API authority in production/API mode. Production/API saves no longer write cleaning compatibility KV mirrors, and staging retired all matched cleaning zone/round mirrors after guarded dry-run proof. Public QR zone listing and public complaint zone lookup read normalized cleaning zones before falling back to legacy `czone:` KV mirrors when present. Public complaints write normalized complaints without creating new `ccomplaint:*` KV mirrors when the normalized complaints driver is configured.
- PPE now uses normalized API authority in production/API mode. Production/API saves no longer write PPE compatibility KV mirrors, and staging retired all matched PPE mirrors after guarded dry-run proof.
- Staging preflight model coverage and CI-safe dry gate are done. A future deploy-blocking live secret-backed staging gate can still be considered separately.
- Minimal static-analysis gate is done through `npm run lint`. A future ESLint ruleset can still be considered separately once scoped to avoid broad formatting churn.
- Work records now use normalized API authority in production/API mode without production/API `mtask:*` or `mmeet:*` KV mirror writes; staging currently has no work-record residuals to delete. PM now uses normalized API authority without production/API KV mirror writes; staging has no `pm:*` residuals to delete. User management now uses `app_users` authority without production/API `user:*` KV mirror writes, and staging retired all 12 matched user mirrors after guarded dry-run proof, reducing KV records to 0. Fleet now uses normalized API authority without production/API KV mirror writes, and staging retired all 126 matched fleet mirrors after guarded dry-run proof, reducing KV records to 12. Cleaning now uses normalized API authority without production/API KV mirror writes, and staging retired all matched cleaning zone/round mirrors, reducing KV records to 138. Tickets now use normalized API authority without production/API KV mirror writes, production/API ticket photos use `/api/files` plus `file_metadata`, and generic production/API KV writes for `photo:*` are no-ops so older open clients cannot recreate legacy photo records. App config now uses normalized API authority in production/API mode without `config:v1` mirror writes, and staging retired the app-config mirror after guarded dry-run proof, reducing KV records to 184. Settings records (`location:` and `appIssue:`) now use normalized API authority in production/API mode; production/API app issue saves and automatic app issue reports no longer create new `appIssue:*` KV mirrors, and generic production/API KV writes for `location:*` and `appIssue:*` are no-ops so older open clients cannot recreate retired mirrors. Technician/user presence now uses normalized API authority in production/API mode and no longer creates new `presence:*` KV mirrors on production/API saves; generic KV writes for `presence:*` are a no-op in production/API mode so older open clients cannot recreate retired mirrors. On 2026-07-10, staging retired all matched `presence:*` and `appIssue:*` KV mirrors after dry-run proof, reducing KV records from 219 to 189. Phone push subscriptions now use normalized server storage in production/API mode, no longer write `pushSubscriptions:v1`, and staging retired that aggregate legacy key after dry-run proof, reducing KV records to 188. PPE now uses normalized API authority without production/API KV mirror writes, and staging retired all matched PPE mirrors, reducing KV records to 185. The route budget is now 19/24 after grouping AI endpoints into one dynamic route.
